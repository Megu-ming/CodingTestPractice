/*
문제
배열을 정렬하는 것은 쉽다. 수가 주어지면, 그 수의 각 자리수를 내림차순으로 정렬해보자.

입력
첫째 줄에 정렬하려고 하는 수 N이 주어진다. N은 1,000,000,000보다 작거나 같은 자연수이다.

출력
첫째 줄에 자리수를 내림차순으로 정렬한 수를 출력한다.

아이디어
각 자리의 수는 0~9 이므로 size가 10인 int 배열로 각 인덱스의 갯수를 센다.
9부터 각 갯수에 맞게 세면 끝
int num[10]
1. 각 자리를 추출 digit
2. 해당하는 값의 num[digit]++
for(int i = 9; i>=0; i--)
    while(num[i])
        cout<<num[i]--
*/

#include <iostream>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long N;
    cin>>N;
    int num[10] = {0};
    int digit = -1;
    while(N)
    {
        digit = N%10;
        num[digit]++;
        N/=10;
    }

    for(int i = 9; i>=0; i--)
    {
        while(num[i]!=0)
        {
            cout<<i;
            num[i]--;
        }
    }

    return 0;
}