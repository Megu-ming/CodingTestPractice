/*
문제
2차원 평면 위의 점 N개가 주어진다. 
좌표를 x좌표가 증가하는 순으로, x좌표가 같으면 y좌표가 증가하는 순서로 정렬한 다음 출력하는 프로그램을 작성하시오.

입력
첫째 줄에 점의 개수 N (1 ≤ N ≤ 100,000)이 주어진다. 
둘째 줄부터 N개의 줄에는 i번점의 위치 xi와 yi가 주어진다. (-100,000 ≤ xi, yi ≤ 100,000) 좌표는 항상 정수이고, 위치가 같은 두 점은 없다.

출력
첫째 줄부터 N개의 줄에 점을 정렬한 결과를 출력한다.

아이디어
 xi-1 > xi 이면 swap, 만약 xi-1 == xi 이면 yi-1, yi비교해서 swap
버블정렬은 O(n*n) 이라 시간초과
병합정렬이나 힙정렬 써야할듯?
*/

#include <iostream>
#include <vector>
using namespace std;

void MergeRange(vector<pair<int,int>>& arr, vector<pair<int,int>>& tmp, int l, int m, int r)
{
    int i = l;
    int j = m + 1;
    int t = l;

    while(i<=m && j<=r)
    {
        if(arr[i].first < arr[j].first) 
        {
            tmp[t++] = arr[i++];
        }
        else if (arr[i].first == arr[j].first)
        {
            if(arr[i].second < arr[j].second) tmp[t++] = arr[i++];
            else                              tmp[t++] = arr[j++];
        }
        else tmp[t++] = arr[j++];
    }

    while(i<=m) tmp[t++] = arr[i++];
    while(j<=r) tmp[t++] = arr[j++];

    for(int k = l; k <= r; k++) arr[k] = tmp[k];
}

void MergeSort(vector<pair<int,int>>& arr, vector<pair<int,int>>& tmp, int l, int r)
{
    if(l>=r) return;
    int m = l + (r - l)/2;

    MergeSort(arr, tmp, l, m);
    MergeSort(arr, tmp, m+1, r);
    MergeRange(arr, tmp, l, m, r);
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int N;
    cin>>N;

    vector<pair<int,int>> arr(N), tmp(N);
    for(int i =0; i<N;i++)
    {
        cin>>arr[i].first >> arr[i].second;
    }

    MergeSort(arr, tmp, 0, N-1);

    for(int i = 0; i < N; i++)
    {
        cout<< arr[i].first << " " << arr[i].second << "\n";
    }

    return 0;
}