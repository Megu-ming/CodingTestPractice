/*
문제
세준이는 크기가 N×N인 배열 A를 만들었다. 
배열에 들어있는 수 A[i][j] = i×j 이다. 
이 수를 일차원 배열 B에 넣으면 B의 크기는 N×N이 된다. 
B를 오름차순 정렬했을 때, B[k]를 구해보자.

배열 A와 B의 인덱스는 1부터 시작한다.

입력
첫째 줄에 배열의 크기 N이 주어진다. 
N은 1e5보다 작거나 같은 자연수이다. 
둘째 줄에 k가 주어진다. 
k는 min(1e9, N^2)보다 작거나 같은 자연수이다.

출력
B[k]를 출력한다.
*/

#include <iostream>
#include <vector>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long N, K;
    cin >> N >> K;

    long long low = 1, high = K;
    long long ans = 0;

    while(low <= high){
        long long mid = (low + high) / 2;
        long long count = 0;

        // mid보다 작거나 같은 숫자가 몇개인지 count
        for(int i = 1; i <= N; ++i){
            count += min(mid / i, N);
        }

        // count가 K보다 많으면 mid 낮춰보기
        if(count >= K){
            high = mid -1;
            ans = mid;
        }
        // count가 K보다 작으면 mid 높혀보기
        else{
            low = mid + 1;
        }
    }

    cout << ans;

    return 0;
}