/*
문제
지민이는 자신의 저택에서 MN개의 단위 정사각형으로 나누어져 있는 M×N 크기의 보드를 찾았다. 
어떤 정사각형은 검은색으로 칠해져 있고, 나머지는 흰색으로 칠해져 있다. 
지민이는 이 보드를 잘라서 K×K 크기의 체스판으로 만들려고 한다.

체스판은 검은색과 흰색이 번갈아서 칠해져 있어야 한다. 
구체적으로, 각 칸이 검은색과 흰색 중 하나로 색칠되어 있고, 변을 공유하는 두 개의 사각형은 다른 색으로 칠해져 있어야 한다. 
따라서 이 정의를 따르면 체스판을 색칠하는 경우는 두 가지뿐이다. 
하나는 맨 왼쪽 위 칸이 흰색인 경우, 하나는 검은색인 경우이다.

보드가 체스판처럼 칠해져 있다는 보장이 없어서, 지민이는 K×K 크기의 체스판으로 잘라낸 후에 몇 개의 정사각형을 다시 칠해야겠다고 생각했다. 
당연히 K×K 크기는 아무데서나 골라도 된다. 
지민이가 다시 칠해야 하는 정사각형의 최소 개수를 구하는 프로그램을 작성하시오.

입력
첫째 줄에 정수 N, M, K가 주어진다. 
둘째 줄부터 N개의 줄에는 보드의 각 행의 상태가 주어진다. 
B는 검은색이며, W는 흰색이다.

출력
첫째 줄에 지민이가 잘라낸 K×K 보드를 체스판으로 만들기 위해 다시 칠해야 하는 정사각형 개수의 최솟값을 출력한다.

제한
1 ≤ N, M ≤ 2000
1 ≤ K ≤ min(N, M)

(input 1)
4 4 3
BBBB
BBBB
BBBW
BBWB

아이디어
0-based 기준
B로 시작하는 보드
(i, j) % 2 == 0 (B)
(i, j) % 2 == 1 (W)

diff[i][j]
0 1 0 1 
1 0 1 0
0 1 0 0
1 0 0 0
1 <= K <= N,M
ps[c][r] c : 0 -> N - K r : 0 -> M - K
2 2 2   0 1 2
2 2 1
2 1 0
*/

#include <iostream>
#include <vector>
using namespace std;

int N, M, K; // column, row, K

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> N >> M >> K;
    vector<string> board(N);    // 원본 배열
    vector<vector<int>> diff(N, vector<int>(M, 0));  // 변경이 필요한지를 담은 배열 0 이면 변경 x 1이면 변경 필요
    

    for(int c = 0; c < N; ++c){
        cin >> board[c];
    }
    
    // B로 시작하는 보드 기준
    for(int c = 0; c < N; ++c){
        for(int r = 0; r < M; ++r){
            char expected = ((c+r) % 2 == 0) ? 'B' : 'W';
            diff[c][r] = (expected != board[c][r]);
        }
    }

    // ps 구하기
    vector<vector<int>> ps(N + 1, vector<int>(M + 1, 0));     // 누적합
    for(int c = 1; c < N + 1; c++){
        for(int r = 1; r < M + 1; r++){
            ps[c][r] = ps[c][r-1] + ps[c-1][r] - ps[c-1][r-1] + diff[c - 1][r - 1];
        }
    }

    int ans = K * K;
    for(int c = 0; c + K <=N; ++c){
        for(int r = 0; r + K <= M; ++r){
            int sumB = ps[c + K][r + K] - ps[c][r + K] - ps[c+K][r] + ps[c][r];
            int sumW = K*K - sumB;
            ans = min(ans, min(sumB, sumW));
        }
    }

    cout << ans;

    return 0;
}